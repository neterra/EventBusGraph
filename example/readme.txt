# In a console go to the exemple path and use:

python ..\eventbus_graph.py example.cfg

# This will generate example_ebg.html in the out folder.
# Then copy viz.js to the out folder and open example_ebg.html in a browser.